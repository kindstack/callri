; rtp.conf -- restrict the RTP media port range
; Each concurrent call uses 2 ports (RTP + RTCP), so 100 ports = 50 calls max.
; Keep this in sync with the EXPOSE line in Dockerfile and any firewall rules.
[general]
rtpstart=10000
rtpend=10100
strictrtp=yes
icesupport=no
